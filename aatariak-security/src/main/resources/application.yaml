spring:
  main:
    allow-bean-definition-overriding: true

  datasource:
    url: jdbc:postgresql://localhost:5430/aatariak_db
    username: username
    password: password
    driver-class-name: org.postgresql.Driver

  flyway:
    url: jdbc:postgresql://localhost:5430/aatariak_db
    user: username
    password: password
    clean-disabled: false